pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'whoami'
                sh 'df -h >> a.txt'
                echo 'Task Completed'
            }
        }
    }
}
