pipeline {
    agent any

    environment {
        name="Gokul"
        topic="pipeline"
    }
    stages {
        stage('Printing') {
            steps {
                sh '''
                echo "The username is $name"
                echo "The Topic is $topic"
                df -h >> c.txt
                '''
            }
        }
    }
}
